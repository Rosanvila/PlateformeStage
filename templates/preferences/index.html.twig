{% extends 'base.html.twig' %}

{% block blueBanner %}
    <div class="bg-primary">
        <div class="container">
            <h1 class="text-uppercase text-light fs-4 py-2">
                {{ 'preferences.banner' |trans }}
            </h1>
        </div>
    </div>
{% endblock %}

{% block body %}
{{ form_start(form) }}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-6">
            {% include 'components/PreferencesDelete.html.twig' %}
        </div>
        <div class="col-md-6">
            {% include 'components/PreferencesAccount.html.twig' %}
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="card my-2">
                    <div class="card-body">
                        Contenu de la deuxième ligne
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="card my-2">
                    <div class="card-body">
                        Contenu de la troisième ligne
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{ form_end(form) }}
    {% endblock %}
